#include<iostream>

using namespace std;

//first variable are declared with float values
//then they are printed out with the result of the computation
int main()
{
    float a = 2.0;
    float b = 5.0;
    cout<<a<<" divided by "<<b<<" is "<<a/b;
    return 0;
}