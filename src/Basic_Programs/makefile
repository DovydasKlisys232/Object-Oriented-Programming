#used to execute all makefile instructions
all: divide.cpp

#cleans out the bin folder containing the executables
clean:
	rm ../../bin/*.exe

#instructions for creating divide executable file
divide.exe: divide.cpp
	g++ -o ../../bin/divide.exe divide.cpp